<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <style name="transitions" parent="android:Theme.Material">

        <!-- enable window content transitions 默认值为false -->
        <!--如果指定了android:windowEnterTransition
        和android:windowEnterTransition 设不设置都一样-->
        <item name="android:windowContentTransitions">false</item>

        <!-- specify enter and exit transitions这里你可以写自己的Transition-->
        <item name="android:windowEnterTransition">
            @android:transition/explode</item>

        <item name="android:windowExitTransition">
            @android:transition/explode
        </item>
        <!-- activity执不执行动画，false不执行动画，设置什么都没用 -->
        <item name="android:windowActivityTransitions">true</item>
        <!-- specify shared element transitions -->
        <!--
        <item name="android:windowSharedElementEnterTransition">
            @transition/change_image_transform</item>

         <item name="android:windowSharedElementExitTransition">
            @transition/change_image_transform
          </item>
        -->
    </style>
</resources>
